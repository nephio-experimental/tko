ARG ALPINE_VERSION=latest

FROM alpine:$ALPINE_VERSION

COPY work/tko-preparer /opt/

RUN \
apk add --no-cache libc6-compat tzdata && \
adduser --disabled-password tko

USER tko
ENTRYPOINT [ "/opt/tko-preparer", "start", "-v" ]
